package com.udacity.asteroidradar.data

/**
 * Enum Class: Contains the filter parameter will be used when menu item has been clicked.
 * */
enum class AsteroidAPIFilter {
    TODAY,
    WEEK,
    SAVED
}