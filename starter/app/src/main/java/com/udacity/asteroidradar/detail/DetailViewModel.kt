package com.udacity.asteroidradar.detail

import androidx.lifecycle.ViewModel

/**
 * The view model to store data in detail fragment.
 * */
class DetailViewModel : ViewModel() {
    init {

    }

    override fun onCleared() {
        super.onCleared()
    }
}